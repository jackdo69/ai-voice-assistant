{
  "name": "ai-voice-assistant",
  "version": "0.1.0",
  "scripts": {
    "install:web": "cd frontend && npm install",
    "build:web": "cd frontend && npm run build"
  },
  "devDependencies": {
    "@types/node": "^20.1.7",
    "ts-node": "^10.9.1",
    "typescript": "~5.0.4"
  },
  "dependencies": {
    "prettier": "^3.0.3"
  }
}