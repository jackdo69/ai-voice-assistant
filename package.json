{
  "name": "ai-voice-assistant",
  "version": "0.1.0",
  "scripts": {
    "install:web": "cd frontend && npm install",
    "build:web": "cd frontend && npm run build",
    "deploy:lambda": "cd backend && npm run cdk deploy"
  },
  "dependencies": {
    "prettier": "^3.0.3"
  }
}